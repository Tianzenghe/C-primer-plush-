#include<iostream>
using namespace std;
int main()
{
	char ch;
	int count = 0;
	cin >> ch;
	while (ch != '#') {
		cout << ch;
		++count;
		cin >> ch;//这一步极为重要,没有这一步程序将会一直执行第一个字符!
	}
	cout << endl << count << endl << ch;

	return 0;
}
